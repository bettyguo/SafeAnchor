# ===========================================================================
# Reproduce Table 1 (Llama-2-7B-Chat)
# Sequence: Medical → Legal → Code
# ===========================================================================

defaults:
  - /config
  - /model: llama2
  - /training: default
  - _self_

seed: 42
domain_order: [medical, legal, code]

experiment_name: reproduce_table1_llama2_seed${seed}

wandb:
  enabled: false
  tags: [table1, llama2, reproduce]
